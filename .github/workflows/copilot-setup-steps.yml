name: "Copilot Setup Steps"

on:
  workflow_dispatch:

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v7

      - uses: AtlassianPS/AtlassianPS.Standards/.github/actions/setup-powershell@72a8008dcd2f840b373d22380a50bc5f1870211f # v0.3.3
