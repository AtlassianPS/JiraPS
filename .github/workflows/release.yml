name: Create Release and Publish

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    name: "Release"
    runs-on: "ubuntu-latest"
    permissions:
      actions: read
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Validate release tag
        id: release_ref
        uses: AtlassianPS/AtlassianPS.Standards/.github/actions/resolve-release-tag@b95fd4d1ad0fca24905a38be4db70da7eeff8cb0 # v0.1.12

      - name: Download artifact from commit that got tagged
        uses: dawidd6/action-download-artifact@v21
        with:
          workflow: ci.yml
          workflow_conclusion: success
          commit: ${{ steps.release_ref.outputs.release_sha }}
          name: Release
          path: ./Release/
          if_no_artifact_found: fail

      - uses: AtlassianPS/AtlassianPS.Standards/.github/actions/setup-powershell@b95fd4d1ad0fca24905a38be4db70da7eeff8cb0 # v0.1.12

      - uses: AtlassianPS/AtlassianPS.Standards/.github/actions/build-release-notes@b95fd4d1ad0fca24905a38be4db70da7eeff8cb0 # v0.1.12
        id: release_notes
        with:
          release-version: ${{ steps.release_ref.outputs.release_tag }}

      - name: Publish module
        run: |
          Invoke-Build -Task Publish `
            -VersionToPublish ${{ steps.release_ref.outputs.release_tag }} `
            -PSGalleryAPIKey ${{ secrets.PSGALLERY_API_KEY }}
        shell: pwsh

      - name: Create Release and Upload Asset
        uses: softprops/action-gh-release@v3
        with:
          body_path: ${{ steps.release_notes.outputs.release_notes_path }}
          files: ./Release/JiraPS.zip
          fail_on_unmatched_files: true
          draft: false
          tag_name: ${{ steps.release_ref.outputs.release_tag }}
          name: ${{ steps.release_ref.outputs.release_tag }}
          prerelease: ${{ contains(steps.release_ref.outputs.release_tag, 'alpha') || contains(steps.release_ref.outputs.release_tag,
            'beta') || contains(steps.release_ref.outputs.release_tag, 'rc') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
